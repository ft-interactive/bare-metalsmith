{{> head}}
  <body>
    {{{contents}}}
    <!-- Parse "feed" data object, iterating over the array -->
    {{#each feed}}
      <h2>{{company}}</h2>
      <p>
        {{copy}}
      </p>
    {{/each}}
  </body>
</html>
